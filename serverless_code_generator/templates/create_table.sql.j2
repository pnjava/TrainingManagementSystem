CREATE TABLE {{ table.name }} (
{% for col in table.columns %}  {{ col.name }} {{ col.type }}{% if not col.nullable %} NOT NULL{% endif %}{% if col.name == table.primary_key %} PRIMARY KEY{% endif %},{% endfor %}
);
